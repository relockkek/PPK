<Window x:Class="WpfApp20.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp20"
        mc:Ignorable="d"
        xmlns:models="clr-namespace:WpfApp20.Models"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type models:Student}">
            <StackPanel>
                <TextBlock Text="{Binding FIO, StringFormat=ФИО: {0}}"/>
                <TextBlock Text="{Binding Group, StringFormat=Группа: {0}}"/>
                <TextBlock Text="{Binding Weight, StringFormat=Вес: {0}}"/>
            </StackPanel>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:Groups}">
            <StackPanel>
                <Label Content="{Binding Name, StringFormat=Специальность: {0}}"/>
                <TextBlock Text="{Binding Cource, StringFormat=Курс: {0}}"/>
                <TextBlock Text="{Binding NumOfStud, StringFormat=Количество студентов в группе: {0}}"/>
            </StackPanel>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:Curator}">
            <StackPanel>
                <Label Content="{Binding FIO, StringFormat=ФИО: {0}}"/>
                <TextBlock Text="{Binding AdminGroup, StringFormat=Владения куратора: {0}}"/>
                <TextBlock Text="{Binding Weight, StringFormat=Вес: {0}}"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="Студенты" Click="GetStudentList"/>
            <Button Content="Группы" Click="GetGroupList"/>
            <Button Content="Кураторы" Click="GetCuratorList"/>
            <Button Content="Редактор выделенной записи" Click="EditItem"/>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding Item}"/>
    </Grid>
</Window>
